import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ouroboros.neuralemergence',
  appName: 'ouroboros-neural-emergence',
  webDir: 'dist'
};

export default config;
